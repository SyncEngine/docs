# Boolean Field (`type: boolean`)

Alias of `toggle`.

Use this when you want a semantic boolean type name in config while using toggle behavior.

```php
'enabled' => [
    'label' => $this->trans('Enabled'),
    'type' => 'boolean',
]
```
